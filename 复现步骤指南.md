# SimLingo 项目复现详细步骤指南

本指南提供了完整的步骤和命令来复现 SimLingo 项目的训练和评估。

## 目录
1. [环境准备](#环境准备)
2. [CARLA 安装](#carla-安装)
3. [代码仓库克隆](#代码仓库克隆)
4. [Conda 环境配置](#conda-环境配置)
5. [环境变量设置](#环境变量设置)
6. [数据集下载](#数据集下载)
7. [训练模型](#训练模型)
8. [评估模型](#评估模型)
9. [常见问题解决](#常见问题解决)

---

## 环境准备

### 系统要求
- **操作系统**: Linux (推荐 Ubuntu 18.04 或更高版本)
- **GPU**: NVIDIA GPU (推荐 RTX 2080Ti 或更高)
- **CUDA**: 与 PyTorch 2.2.0 兼容的版本
- **Python**: 3.8
- **磁盘空间**: 
  - Mini 数据集: ~4GB
  - Base 数据集: ~400GB
  - Full 数据集: ~4TB
- **内存**: 至少 32GB RAM

---

## CARLA 安装

### 步骤 1: 创建 CARLA 目录并下载

```bash
# 创建 CARLA 安装目录
mkdir -p ~/software/carla0915
cd ~/software/carla0915

# 下载 CARLA 0.9.15
wget https://carla-releases.s3.us-east-005.backblazeb2.com/Linux/CARLA_0.9.15.tar.gz

# 解压
tar -xvf CARLA_0.9.15.tar.gz

# 删除压缩包以节省空间
rm CARLA_0.9.15.tar.gz
```

### 步骤 2: 下载额外地图

```bash
# 进入 Import 目录
cd Import

# 下载额外地图
wget https://carla-releases.s3.us-east-005.backblazeb2.com/Linux/AdditionalMaps_0.9.15.tar.gz

# 返回上级目录并导入资源
cd ..
bash ImportAssets.sh
```

### 步骤 3: 测试 CARLA 是否正常运行

```bash
# 启动 CARLA 服务器 (后台运行)
cd ~/software/carla0915
./CarlaUE4.sh -quality-level=Low -RenderOffScreen

# 在另一个终端测试连接
cd ~/software/carla0915/PythonAPI/examples
python3 spawn_npc.py
```

如果看到车辆生成，说明 CARLA 安装成功。按 `Ctrl+C` 停止服务器。

---

## 代码仓库克隆

```bash
# 克隆仓库
git clone https://github.com/zhangjenny/simlingo.git
cd simlingo

# 或使用原始仓库
# git clone https://github.com/RenzKa/simlingo.git
# cd simlingo
```

---

## Conda 环境配置

### 步骤 1: 创建基础环境

```bash
# 使用提供的 environment.yaml 创建环境
conda env create -f environment.yaml

# 激活环境
conda activate simlingo
```

### 步骤 2: 安装 PyTorch

```bash
# 安装 PyTorch 2.2.0 (确保使用正确的 CUDA 版本)
pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0
```

### 步骤 3: 安装 Flash Attention

```bash
# 安装 flash-attn (这可能需要一些时间)
pip install flash-attn==2.7.0.post2 --no-build-isolation
```

### 步骤 4: 验证安装

```bash
# 测试 PyTorch
python -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}')"

# 测试 CARLA Python API
python -c "import carla; print(f'CARLA版本: {carla.__version__}')"
```

---

## 环境变量设置

### 创建环境变量配置文件

每次使用前需要设置环境变量，建议创建一个配置脚本：

```bash
# 创建配置脚本
cat > ~/simlingo_env.sh << 'EOF'
#!/bin/bash

# 设置 CARLA 路径
export CARLA_ROOT=~/software/carla0915

# 设置工作目录
export WORK_DIR=~/simlingo

# 设置 Python 路径
export PYTHONPATH=$PYTHONPATH:${CARLA_ROOT}/PythonAPI/carla
export PYTHONPATH=$PYTHONPATH:${CARLA_ROOT}/PythonAPI/carla/dist/carla-0.9.15-py3.8-linux-x86_64.egg
export PYTHONPATH=$PYTHONPATH:${WORK_DIR}

# 设置场景运行器路径
export SCENARIO_RUNNER_ROOT=${WORK_DIR}/scenario_runner
export LEADERBOARD_ROOT=${WORK_DIR}/leaderboard
export PYTHONPATH="${CARLA_ROOT}/PythonAPI/carla/":"${SCENARIO_RUNNER_ROOT}":"${LEADERBOARD_ROOT}":${PYTHONPATH}

# 如果在中国，设置 HuggingFace 镜像
export HF_ENDPOINT=https://hf-mirror.com

echo "SimLingo 环境变量已设置完成"
EOF

# 使配置脚本可执行
chmod +x ~/simlingo_env.sh
```

### 使用配置脚本

```bash
# 每次使用前运行
source ~/simlingo_env.sh
```

---

## 数据集下载

### 选项 1: 下载 Mini 数据集 (推荐用于测试)

```bash
# 创建数据集目录
mkdir -p database/simlingo

# 使用提供的脚本下载 Mini 数据集
cd simlingo
bash download_mini.sh
```

### 选项 2: 从 HuggingFace 下载完整数据集

```bash
# 安装 Git LFS (如果尚未安装)
git lfs install

# 克隆数据集仓库
git clone https://huggingface.co/datasets/RenzKa/simlingo

# 进入目录
cd simlingo

# 拉取 LFS 文件
git lfs pull
```

### 选项 3: 下载单个文件

```bash
# 下载特定文件 (替换 [filename] 为实际文件名)
wget https://huggingface.co/datasets/RenzKa/simlingo/resolve/main/[filename].tar.gz
```

### 解压数据集

```bash
# 创建输出目录
mkdir -p database/simlingo

# 解压所有 .tar.gz 文件到同一目录
for file in *.tar.gz; do
    echo "正在解压 $file 到 database/simlingo/..."
    tar -xzf "$file" -C database/simlingo/
done

echo "数据集解压完成"
```

### 验证数据集

```bash
# 检查数据集结构
ls -lh database/simlingo/

# 运行测试脚本验证数据集
python test_dataset_loading.py
```

---

## 训练模型

### 准备工作

1. **登录 Weights & Biases (可选，用于训练日志)**

```bash
# 安装 wandb (已包含在 environment.yaml 中)
# 登录
wandb login
```

2. **检查配置文件**

```bash
# 查看训练配置
cat simlingo_training/config/experiment/simlingo_seed1.yaml

# 确保数据路径正确
# 编辑配置文件中的数据路径
nano simlingo_training/config/experiment/simlingo_seed1.yaml
```

### 训练 SimLingo-Base 模型

```bash
# 激活环境并设置环境变量
conda activate simlingo
source ~/simlingo_env.sh

# 进入训练目录
cd simlingo_base_training

# 开始训练 (单 GPU)
python train.py

# 多 GPU 训练
python train.py gpus=4
```

### 训练完整 SimLingo 模型

```bash
# 确保在项目根目录
cd ~/simlingo

# 单机多卡训练 (例如 8 卡)
python simlingo_training/train.py \
    experiment=simlingo_seed1 \
    data_module.batch_size=8 \
    gpus=8 \
    name=simlingo_seed1 \
    data_module.base_dataset.data_path=/path/to/database/simlingo
```

### SLURM 集群训练 (如果使用集群)

```bash
# 编辑 SLURM 脚本
nano train_simlingo_seed1.sh

# 修改以下内容:
# - 第 7 行: 设置正确的输出路径
# - 第 8 行: 设置正确的错误日志路径
# - 第 9 行: 设置正确的分区名称
# - 第 18 行: 设置正确的 conda 环境路径
# - 第 34 行: 设置正确的数据路径

# 提交任务
sbatch train_simlingo_seed1.sh
```

### 监控训练

```bash
# 查看 wandb 日志 (在浏览器中)
# 访问 https://wandb.ai 查看训练进度

# 或查看本地日志
tail -f results/logs/simlingo_*.out
```

---

## 评估模型

### 下载预训练模型

```bash
# 从 HuggingFace 下载模型
mkdir -p checkpoints
cd checkpoints

# 使用 git clone
git clone https://huggingface.co/RenzKa/simlingo

# 或使用 wget 下载特定检查点
wget https://huggingface.co/RenzKa/simlingo/resolve/main/[checkpoint_name]
```

### Bench2Drive 闭环驾驶评估

#### 步骤 1: 准备评估环境

```bash
# 确保环境变量已设置
source ~/simlingo_env.sh

# 检查 Bench2Drive 数据集
ls -l Bench2Drive/
```

#### 步骤 2: 配置评估脚本

```bash
# 编辑评估启动脚本
nano start_eval_simlingo.py

# 需要修改的配置 (查找 TODO 标记):
# - carla_root: CARLA 安装路径
# - repo_root: SimLingo 仓库路径
# - checkpoint: 模型检查点路径
# - agent_file: 代理文件路径
# - partition_name: SLURM 分区名 (如果使用 SLURM)
```

#### 步骤 3: 运行评估 (本地单 GPU)

```bash
# 设置环境变量
export CARLA_ROOT=~/software/carla0915
export WORK_DIR=~/simlingo
export PYTHONPATH=$PYTHONPATH:${WORK_DIR}/Bench2Drive/leaderboard
export PYTHONPATH=$PYTHONPATH:${WORK_DIR}/Bench2Drive/scenario_runner

# 运行单个路线测试
cd Bench2Drive

bash leaderboard/scripts/run_evaluation_debug.sh
```

#### 步骤 4: SLURM 集群并行评估

```bash
# 运行评估脚本
python start_eval_simlingo.py

# 脚本会自动提交多个 SLURM 任务并行评估 220 条路线
```

#### 步骤 5: 合并结果并计算指标

```bash
# 等待所有评估完成 (确保有 220 条路线的结果)

# 合并 JSON 结果
python Bench2Drive/tools/merge_route_json.py -f /path/to/results/

# 计算多能力指标
python Bench2Drive/tools/ability_benchmark.py -r merge.json

# 计算驾驶效率和平滑度
python Bench2Drive/tools/efficiency_smoothness_benchmark.py -f merge.json -m /path/to/metrics/
```

### 语言能力评估

#### 步骤 1: 设置 OpenAI API

```bash
# 编辑 GPT 评估配置
nano simlingo_training/utils/gpt_eval.py

# 添加你的 OpenAI API 密钥
# OPENAI_API_KEY = "your-api-key-here"
```

#### 步骤 2: VQA 评估

```bash
# 编辑评估脚本
nano simlingo_training/eval.py

# 设置 eval_mode = "QA"

# 运行 VQA 评估
cd simlingo_training
python eval.py

# 计算指标
python eval_metrics.py
```

#### 步骤 3: Commentary 评估

```bash
# 设置 eval_mode = "commentary"
nano simlingo_training/eval.py

# 运行 commentary 评估
python eval.py

# 计算指标
python eval_metrics.py
```

#### 步骤 4: Dreaming 评估

```bash
# 设置 eval_mode = "Dreaming"
nano simlingo_training/eval.py

# 运行 Dreaming 评估
python eval.py

# 计算指标
python eval_metrics.py
```

---

## 常见问题解决

### 1. CARLA 无法启动

**问题**: CARLA 启动后立即退出

**解决方案**:
```bash
# 检查 Vulkan
/usr/bin/vulkaninfo | head -n 5

# 重新安装 Vulkan 工具
sudo apt install vulkan-tools vulkan-utils

# 尝试降级 NVIDIA 驱动到 470 版本
# 或使用离屏渲染
./CarlaUE4.sh -RenderOffScreen -quality-level=Low
```

### 2. 端口冲突

**问题**: CARLA 端口被占用

**解决方案**:
```bash
# 检查端口占用
lsof -i:2000
lsof -i:2001

# 杀死占用进程
kill -9 <PID>

# 或使用清理脚本
bash Bench2Drive/tools/clean_carla.sh
```

### 3. CUDA 内存不足

**问题**: 训练时 GPU 内存溢出

**解决方案**:
```bash
# 减小 batch size
python simlingo_training/train.py data_module.batch_size=4

# 使用梯度累积
python simlingo_training/train.py data_module.batch_size=4 accumulate_grad_batches=2

# 使用混合精度训练
python simlingo_training/train.py precision=16
```

### 4. HuggingFace 下载慢或失败

**问题**: 无法访问 HuggingFace 或下载速度慢

**解决方案**:
```bash
# 使用镜像站点
export HF_ENDPOINT=https://hf-mirror.com

# 使用 huggingface-cli 断点续传
huggingface-cli download --repo-type dataset --resume-download RenzKa/simlingo --local-dir ./database/simlingo
```

### 5. 环境依赖问题

**问题**: 某些包无法安装

**解决方案**:
```bash
# 单独安装问题包
pip install <package_name> --no-cache-dir

# Flash-attention 编译失败
# 确保有足够的 RAM (至少 16GB)
pip install flash-attn==2.7.0.post2 --no-build-isolation

# 如果还是失败，可以跳过 (会降低训练速度但不影响功能)
# 注释掉 environment.yaml 中的 flash-attn
```

### 6. 数据集路径问题

**问题**: 训练时找不到数据集

**解决方案**:
```bash
# 检查数据集结构
ls -l database/simlingo/

# 确保路径正确
# 编辑配置文件中的 data_path
nano simlingo_training/config/config.py

# 或在命令行指定
python simlingo_training/train.py data_module.base_dataset.data_path=/absolute/path/to/database/simlingo
```

### 7. 评估崩溃

**问题**: Bench2Drive 评估时 CARLA 崩溃

**解决方案**:
```bash
# 增加 sleep 时间
# 编辑 Bench2Drive/leaderboard/leaderboard/leaderboard_evaluator.py
# 增加第 207 行的 sleep 时间

# 使用自动重启脚本
# 创建无限循环重启评估直到完成
while true; do
    python start_eval_simlingo.py
    if [ $? -eq 0 ]; then
        break
    fi
    sleep 10
done
```

### 8. 清理 CARLA 进程

**问题**: CARLA 进程残留

**解决方案**:
```bash
# 使用提供的清理脚本
bash Bench2Drive/tools/clean_carla.sh

# 手动清理
ps aux | grep Carla | awk '{print $2}' | xargs kill -9
ps aux | grep carla | awk '{print $2}' | xargs kill -9

# 清理僵尸进程
ps aux | grep defunct
```

---

## 快速开始检查清单

- [ ] 安装 CARLA 0.9.15
- [ ] 克隆 SimLingo 仓库
- [ ] 创建 conda 环境
- [ ] 安装 PyTorch 2.2.0
- [ ] 安装 flash-attn
- [ ] 设置环境变量
- [ ] 下载数据集 (至少 Mini 版本)
- [ ] 测试数据集加载
- [ ] 运行一次训练测试
- [ ] 下载预训练模型
- [ ] 运行一次评估测试

---

## 有用的命令参考

### 检查系统状态

```bash
# 检查 GPU
nvidia-smi

# 检查 CUDA 版本
nvcc --version

# 检查磁盘空间
df -h

# 检查内存
free -h
```

### 监控训练

```bash
# 监控 GPU 使用
watch -n 1 nvidia-smi

# 监控日志
tail -f results/logs/*.out

# 监控训练进程
ps aux | grep python
```

### 数据集管理

```bash
# 统计数据集大小
du -sh database/simlingo/

# 查找 .json.gz 文件
find database/simlingo/ -name "*.json.gz" | wc -l

# 验证数据完整性
python dataset_generation/count_json_gz_files.py
```

---

## 更多资源

- **论文**: https://arxiv.org/abs/2503.09594
- **项目主页**: https://www.katrinrenz.de/simlingo/
- **数据集**: https://huggingface.co/datasets/RenzKa/simlingo
- **模型**: https://huggingface.co/RenzKa/simlingo
- **视频演示**: https://www.youtube.com/watch?v=Mpbnz2AKaNA
- **GitHub Issues**: https://github.com/RenzKa/simlingo/issues

---

## 引用

如果您使用了本项目，请引用：

```bibtex
@InProceedings{Renz2025cvpr,
  title={SimLingo: Vision-Only Closed-Loop Autonomous Driving with Language-Action Alignment},
  author={Renz, Katrin and Chen, Long and Arani, Elahe and Sinavski, Oleg},
  booktitle={Conference on Computer Vision and Pattern Recognition (CVPR)},
  year={2025}
}
```

---

**最后更新**: 2025-12-28

**维护者**: SimLingo 团队

如有问题，请在 GitHub Issues 中提出或参考原始 README.md 文件。
